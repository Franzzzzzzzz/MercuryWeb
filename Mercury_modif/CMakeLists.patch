--- ../../MercuryDPM/MercurySource/Drivers/Tutorials/Advanced/CMakeLists.txt	2021-02-18 17:58:07.586871150 +1100
+++ CMakeLists.txt	2021-02-19 19:17:13.763200994 +1100
@@ -5,6 +5,7 @@
 #########################################
 #Collect all the names of the cpp, note at the moment limited to demos, but will be fixed later
 file(GLOB CPPFILES  "*.cpp")
+
 #for every cpp found
 foreach(CPPFILE ${CPPFILES})
 	#extract the actually file name
@@ -14,5 +15,5 @@
 	#Make the exe
 	add_executable(${EXECNAME} ${FILENAME})
     #All cpp folder and linked agaist MercuryBase
-	target_link_libraries(${EXECNAME}  MercuryBase)
+	target_link_libraries(${EXECNAME}  MercuryBase -lpthread)
 endforeach()
